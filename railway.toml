# Railway configuration for Heka Backend Service
# Uses Dockerfile in root which builds from backend/

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[healthcheck]
path = "/health"
interval = 30
timeout = 10
retries = 3

